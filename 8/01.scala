/**
 * sum: List[Int] => Int 함수의 구현을 명시하는 속성들을 고안하라.
 * 코드만들 필요는 없고 비공식적인 서술로 충분
 * 몇가지 착안 제시
 *
 * - 목록을 뒤집어서 합산해도 원래의 목록(뒤집히지 않은)의 합산과 같은 결과가 나와야한다.
 * - 목록의 모든 요소가 값이 같다면 그 합은 어때야 할까?
 * - 그 외에 다른 속성들은 없을까?
 */